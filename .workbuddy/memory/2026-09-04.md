# 2026-09-04

- Dashboard 侧边栏支持折叠/展开：`src/components/dashboard/sidebar.tsx` 改为带状态的 client 组件，折叠时收窄为图标栏（w-56 ↔ w-14），label 隐藏、hover 显示 title，底部 border-t 处加 PanelLeftClose/PanelLeftOpen 切换按钮，折叠状态持久化到 localStorage `licentra.sidebarCollapsed`，宽度切换带 transition。tsc 类型检查通过。
- 修复 /login hydration mismatch：报错为 `<html>` 上多出 `data-google-analytics-opt-out` 属性，项目代码未注入该属性，判定为浏览器端 Google Analytics Opt-out 类扩展在水合前改动 DOM 所致（环境问题非代码缺陷），给 `src/app/layout.tsx` 的 `<html>` 加 `suppressHydrationWarning` 抑制该元素属性比对。
- 修复 /dashboard/licenses Prisma P2022 `The column '(not available)' does not exist`：Prisma 报错把内部列名隐成 `(not available)`，需读 `meta.driverAdapterError.cause.originalMessage` 才能看到真实列名（本次为 `column Activation.lastCheckInAt does not exist`）。根因是 `prisma generate` 落后于 schema：旧生成产物 Activation 模型还含 `lastCheckInAt`/`lastCheckInBrowser`，但当前 schema 与 DB 都已不含该列（仅保留 `browser`），`include: { activations: ... }` 时旧 client 生成 SELECT 引用了不存在的列。解决：`pnpm exec prisma generate` 重生成。诊断用了临时脚本 `scripts/inspect-db.cjs` 与 `scripts/probe-licenses.cjs`，完成后已删除。
- /dashboard/licenses 页 Search 卡片加手动 Refresh 按钮：新增 `licenses-refresh-button.tsx`（client 组件，outline 按钮 + RefreshCw 图标，点击 `startTransition(router.refresh)` 重取列表，pending 时图标 spin、文案变 Refreshing…），Search 卡片 CardHeader 改为 flex row 布局（标题/描述左侧、按钮右侧），与 product 详情页卡片头部一致。tsc 通过。
- License 详情页（`licenses/[id]/page.tsx`）顶部加回退链接：用户经确认选择"固定返回列表页"而非 router.back()，实现为 `<Link href="/dashboard/licenses">` + ArrowLeft 图标 + "Back to licenses"（muted 文字、hover 加深），纯服务端组件。tsc 通过。
- 调研 licenses 列表页 "Last check-in" 列可扩展信息（用户决定先不做）：DB 10 条 Activation / 5 张 license，browser/ipAddress/label 全非空。列表列现只显示 max(lastCheckedAt) 时间，但该行还带 browser（"Edge 152 · macOS"）、ipAddress、label（客户自填设备名，可与 browser 不一致）、fingerprint。语义注意：check-in 接口只更新 lastCheckedAt 不刷新 ip/browser/label；activate 重复指纹才刷新。产品 signatureTtlSeconds（默认 86400）可作"离线签章过期"信号。候选方案：Last check-in 时间下加设备行、与 Last activated 对称、stale 弱化。
- Emailed 状态从列表页移到详情页：`licenses/page.tsx` 删除 "Emailed" 表头与该单元格（badge sent/pending 判定逻辑同前，仍在 Status 等列用 Badge）；`licenses/[id]/page.tsx` Status 卡片在 "Emailed at" 上一行新增 Row "Emailed"（sent→success badge / pending→destructive badge，判定条件 emailedAt 是否非空）。tsc 通过。
- 修复 Product 编辑页（长内容页）侧边栏收起按钮"消失"：aside 原为普通 flex 子项随内容高度拉伸，按钮被推出首屏。改为 `sticky top-0 h-screen` + nav `min-h-0 overflow-y-auto`，按钮始终钉在视口底部。tsc 通过。
